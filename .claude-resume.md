Continue work on toolkit plugin tuning enhancement

Working directory: ~/Sites/Global/scott-toolkit

Where we left off: ALL research and design complete. Verified that project-level
`enabledPlugins` works (tested on Bresco platform). Verified deep merge preserves
global permissions. Applied fix to Bresco platform and Eleanor. Ready to implement
toolkit changes via GSD.

Key decisions made across this session:
- Project-level `.claude/settings.json` with `enabledPlugins: false` disables the Vercel plugin (~52K tokens saved)
- Deep merge confirmed: global permissions.deny survives project-level overrides
- Plugin IDs go in interfaces.json `plugins` section (decoupling, not hardcoded)
- `required: true/false` field guards against aggressive plugin disabling (Superpowers, Impeccable = required)
- Session-start hook does bidirectional detection (plugin active on non-Vercel project, OR plugin disabled on Vercel project)
- No standalone `plugin-tune.sh` tool needed (hook warns, manual 4-line JSON is fine)
- `.claude/settings.json` files are committed (not gitignored), following existing project practice
- Legacy projects (life-os, d2d-payroll, automation-business) are being replaced, not a concern
- toolkit-resolve needs `plugins` added to its exclusion filter to avoid false operation matches
- schema_version bumps from 1.0 to 1.1

Files to change (6 files):
- config/interfaces.json - add `plugins` section with id, provider, required, description per plugin
- tools/toolkit-resolve - add `plugins` to grep exclusion filter (line 22)
- rules/claude-behavior.md - add one line about plugin ID resolution from interfaces.json
- hooks/session-start.sh - add bidirectional plugin-project alignment check (~20 lines after line 79)
- workflows/new-project.md - Phase 5: generate .claude/settings.json alongside stack-lock.json
- CHANGELOG.md - document the enhancement

Key files for context:
- references/autonomous-toolkit-research.md - full research (52K token overhead, Vercel plugin architecture)
- docs/v5-unified-design.md - toolkit philosophy (sections 4-5 for primitives and architecture)
- hooks/session-start.sh - where the hook check goes (after stack-lock staleness check, line ~79)

Already done (don't redo):
- ~/Sites/Bresco/code/platform/.claude/settings.json - created and tested
- ~/Sites/Personal/code/eleanor/.claude/settings.json - created

Next step: /scott:new-feature in the toolkit project to implement the plugin tuning enhancement via GSD
