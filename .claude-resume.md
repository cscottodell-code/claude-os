# Session Resume: Toolkit Coherence & Guardrail Hardening

## What's Done
1. **SurrealDB guardrails hardened** (committed: `386352d`)
   - Added "no general knowledge" directive to SKILL.md
   - Added live instance health check to inject-surrealdb-skill.sh
   - Updated checks/surrealdb.json, claude-behavior.md, toolkit-briefing, CHANGELOG v5.1.4
2. **Exhaustive cross-reference audit completed** (not committed, findings only)
   - Every file in the toolkit audited for stale paths, missing registrations, hardcoded values
   - All claims labeled [VERIFIED], [INFERRED], or [ASSUMED]
   - Subagent fabrications caught and corrected

## 13 Actions Ready to Execute

| # | Action | Category |
|---|--------|----------|
| 1 | Fix `templates/` -> `context/` in 4 workflows + 2 templates | Stale paths |
| 2 | Fix `~/scott-toolkit/` -> `~/Sites/Global/scott-toolkit/` in 3 workflows + 1 template | Stale paths |
| 3 | Fix `skills/reference/` -> current skill system in CLAUDE-MD-TEMPLATE (lines 49-59) | Stale paths |
| 4 | Fix `scott-toolkit-instructions.md` -> `docs/user-guide.md` in toolkit-update.md | Stale paths |
| 5 | Fix stale paths in 3 project CLAUDE.md files (d2d-payroll, spotio-cf, life-os) | Stale paths |
| 6 | Register `check-file-test-trigger.sh` + `uiux-reminder.sh` in settings.json (PostToolUse, Edit\|Write) | Missing hooks |
| 7 | Standardize settings.json hooks to all use symlink paths (`~/.claude/hooks/`) | Path consistency |
| 8 | Fix `session-start.sh` line 88 to use `SCOTT_TOOLKIT_DIR` env var pattern | Path resilience |
| 9 | Make setup.sh verification dynamic (scan dirs instead of static lists) | Setup completeness |
| 10 | Add `--update-paths` to setup.sh for toolkit relocation (find-and-replace canonical path in all .md files) | Path resilience |
| 11 | Delete `tools/toolkit-resolve` + add grep tip to README Contributing Rules | Dead code |
| 12 | Build `toolkit-coherence-check.sh` PostToolUse hook (fires on any toolkit file edit, validates cross-refs) | Prevention |
| 13 | Update CHANGELOG v5.2 + expand version-manifest with new tracked files | Tracking |

## Key Decisions
- `toolkit-resolve` is dead code (zero runtime invocations). Delete it, mitigate with grep tip in README.
- Settings.json should use symlink paths exclusively (not direct toolkit paths)
- All hooks should use `SCOTT_TOOLKIT_DIR` env var with fallback for relocatability
- Setup.sh should handle markdown path updates on relocation (--update-paths flag)
- SurrealDB port 8000 stays hardcoded (infrastructure constant, not worth abstracting)
- Setup.sh verify lists should scan directories dynamically
- `check-file-test-trigger.sh` and `uiux-reminder.sh` were unintentionally never registered in settings.json

## Verified Stale Paths (exact files and lines)
- workflows/new-project.md lines 102, 107, 158, 160 (templates/ + FILE-STRUCTURE-TEMPLATE)
- workflows/retro.md line 109 + 8 shorthand paths (~/scott-toolkit/)
- workflows/log-error.md lines 38, 105, 108, 145, 156 (~/scott-toolkit/ shorthand)
- workflows/log-success.md lines 98, 127, 136 (~/scott-toolkit/ shorthand)
- workflows/toolkit-update.md Phase 5 references scott-toolkit-instructions.md (renamed to docs/user-guide.md in v5)
- context/CLAUDE-MD-TEMPLATE.md lines 49-59 (skills/reference/ deleted in v4)
- context/PRD-TEMPLATE.md line 362 (templates/ -> context/)
- context/RETRO-TEMPLATE.md line 134 (templates/ -> context/)
- Advosy/d2d-payroll/CLAUDE.md line 36, Personal/life-os/CLAUDE.md lines 30-32, Advosy/spotio-cf/CLAUDE.md line 31

## Key Files
- This resume file for full context
- config/version-manifest.json - needs expansion
- config/interfaces.json - lenses section (no changes needed)
- hooks/ - 18 hooks on disk, 1 missing from setup.sh verify, 2 missing from settings.json
- tools/ - 5 on disk (including toolkit-resolve to delete), 2 missing from setup.sh verify

## Next Step
Execute all 13 actions. Start with actions 1-5 (stale path fixes) as parallel subagents since they're independent file edits. Then 6-8 (hook fixes), then 9-12 (infrastructure), then 13 (tracking).
