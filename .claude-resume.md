# Resume Context
_Last updated: 2026-03-03_

## Where we are
Toolkit v2 restructure — core implementation COMPLETE, design discussions in progress.

## What's done
- Full directory restructure (context/, knowledge/, workflows/, hooks/, rules/)
- SurrealDB skills consolidated (surrealdb-v3 + surrealql → surrealdb.md)
- Debug workflow removed, behavior rules restructured (3-system delegation)
- resume-project and new-feature slimmed to orchestrators (delegate to GSD)
- 3 new session hooks: session-start.sh, pre-compact.sh, session-end.sh
- setup.sh deployment script with symlinks
- All hooks deployed, rules symlinked, skills regenerated
- Resume file system: hooks prompt Claude to write .claude-resume.md, session-start rebuilds ACTIVE-PROJECTS.md
- All 8 workflow checklists updated with resume file instruction

## What's NOT committed
All changes are staged/unstaged in git — nothing committed yet. Run `git status` in ~/Sites/scott-toolkit to see full diff.

## What's been implemented
1. **PM mode switching** — CLAUDE.md template has PM Mode field, 4 workflows (new-project, new-feature, resume-project, handoff-to-gary) have GSD/BMAD conditional blocks, behavior rules are mode-aware
2. **Phase auto-advancement** — All 9 workflow files have [STOP]/[AUTO]/[DELEGATE] tags on every phase heading, behavior rules define tag semantics

## What's next (discussed but not implemented)
1. **BMAD PRD template** — Create context/PRD-TEMPLATE-BMAD.md alongside existing GSD template
2. **Smart session-start suggestions** — Hook suggests /scott:new-project vs /scott:resume based on state

## Key decisions
- One toolkit, pluggable PM mode (not two toolkit versions)
- Claude writes resume files (hooks just remind) — only Claude has conversation context
- Phase-end resume files rejected as overkill — context only at risk at session boundaries
- Hooks detect + suggest, Claude decides + writes — each layer does what it's good at
