Continue implementing the v5 toolkit rewrite. Steps 0-5 are complete and committed. Starting Step 6 (Decoupling).

Working directory: ~/Sites/Global/scott-toolkit

Where we left off: Steps 0-5 committed (9 commits ahead of origin). Step 6 (Decoupling) is next. Working tree is clean.

What v5 adds (3 new systems + toolkit rationalization):
1. Stack Enforcement: check files per technology, CLI tools (stack-detect/check/preflight), 3 audit touchpoints, degradation tiers, scoped check loading
2. Learning Loop: tagged lessons, stack-metrics.sh, /scott:stack-review dashboard, human-approved promotion to checks
3. Decoupling: interfaces.json maps 17 abstract operations to concrete commands, toolkit-resolve helper
4. Rationalization: DONE (Steps 0-5 complete)

Steps completed this session:
- Step 0: Rationalization (knowledge/ eliminated, root files cleaned, files moved to correct skills, scope notes added)
- Step 1: Check files (surrealdb, nuxt, tailwind, bun, hono) + stack-lock.schema.json
- Step 2: CLI tools (stack-detect.sh, stack-check.sh, stack-preflight.sh) — smoke tested on Eleanor
- Step 3: Phase-closeout v2.0 (Phase 1.5 stack audit, lesson tagging, test fixtures, check-file-test-trigger hook)
- Step 4: Guard hooks (drift detection, staleness check, uiux-reminder) + setup.sh v5
- Step 5: Workflow updates (new-project stack-lock generation, resume staleness check, execution cycle static checks)

Key decisions made across sessions:
- fix_attempts (not time_to_fix_ms) for measuring suggested fix quality
- Pruning split: 60 days live checks, 90 days static checks
- Scoped check file loading (agents only load relevant tech checks)
- 9-step incremental delivery order (Step 0: rationalize first)
- 17 operations to decouple (v4 only found 12)
- One-place rule: every reference in exactly one location
- guard-npm-install.sh false-positives on its own filename in git commands (use /scott:bypass)
- stack-check.sh needs sed -E for JSON pattern extraction + backslash unescaping

Key files:
- docs/v5-unified-design.md - THE v5 design document (Section 9 = decoupling, Section 13 = delivery order)
- checks/ - 5 technology check files + schema (created in Step 1)
- tools/ - 3 CLI tools (created in Step 2)
- workflows/phase-closeout.md - v2.0 with Phase 1.5 (Step 3)

Next step: Begin Step 6 (Decoupling) from v5-unified-design.md Section 9. Tasks:
- 6a: Create config/interfaces.json (17 abstract operations mapped to concrete commands)
- 6b: Build tools/toolkit-resolve (shell helper)
- 6c: Rewrite rules/claude-behavior.md with abstract refs + stack validation + learning loop
- 6d: Update all workflow files to use abstract operation names
- 6e: Extend stack-preflight.sh for provider health check
