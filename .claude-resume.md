Continue designing the scott-toolkit rewrite (stack enforcement + learning loop + decoupling)

Working directory: ~/Sites/Global/scott-toolkit

Where we left off: Stack enforcement is fully designed (38 considerations, simplified with BOPs). Learning loop (Topic A) and decoupling (Topic C) are NOT yet designed. A full design document was exported for Claude Cowork review before continuing.

Key decisions made this session:
- Check files (one per technology) are the single source of truth, no separate manifest
- stack-lock.json per project records approved stack + exceptions + paths
- Three audit touchpoints: planning (context loading), executing (CLI static + smart agent dispatch), closeout (delta + live integration)
- Agent splitting: by task type first (specialization), then technology (tokens), then scope (rare)
- Tool resolution: CLI first, MCP if CLI can't, Context7 for docs, agent reasoning last
- Three audit outcomes: PASS, FAIL, ACCEPT (intentional deviation recorded in stack-lock)
- Four degradation tiers: Full, Reduced, Minimal, Bypass
- Existing guard-phase-completion.sh is the only gate (audit lives inside phase-closeout)
- 6-step incremental delivery order (each step independently useful)
- BOPs aligned: EIB protocol applied, value stream mapped, 5 duplications removed

Key files:
- docs/toolkit-rewrite-design.md - FULL design document (feed to Cowork for review)
- workflows/phase-closeout.md - the workflow that gets the Phase 1.5 addition
- hooks/guard-phase-completion.sh - existing gate pattern to reuse
- hooks/context-reminders.sh - existing hook pattern for stack detection
- CHANGELOG.md - current version is v2.14.0

Also completed this session:
- Bresco phase closeout finished (lesson #15 added to tasks/lessons.md, gate marker confirmed)

Next steps:
1. Feed docs/toolkit-rewrite-design.md into Claude Cowork for review
2. After Cowork review: design Learning Loop (Topic A) and Decoupling (Topic C)
3. Then proceed to implementation planning via /scott:update-toolkit
