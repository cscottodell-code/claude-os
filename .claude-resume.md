Continue research on autonomous toolkit evolution

Working directory: ~/Sites/Global/scott-toolkit

Where we left off: ALL research complete. 26 subagents + 12 verification runs across 2 sessions.
Original 5 open questions resolved. Then Scott challenged whether we'd fully explored Max plan
features and toolkit philosophy alignment. Deep-dived into both. Found first-party features
(RemoteTrigger, CronCreate, auto mode) that invalidate the original wrapper-script architecture.
NO design or implementation work has started yet.

Key decisions made across sessions:
- Scott wants plan-all-phases-first, then autonomous execution across multiple sessions
- No Anthropic API in the build process (everything uses Max subscription quota)
- Telegram via /scott:remind is the notification channel (iMessage self-send broken on Tahoe)
- Architecture must respect toolkit philosophy: 4-system separation, hook enforcement, decoupling via interfaces.json, two-tier knowledge hierarchy
- A bash wrapper script would VIOLATE toolkit philosophy (creates parallel execution path outside GSD)

Critical findings from this session:
- `--permission-mode auto` WORKS on Max plan (tested, confirmed)
- RemoteTrigger exists (cloud-hosted scheduled agents) but runs in Anthropic cloud, NOT local. Cannot access toolkit hooks, .planning/, local SurrealDB. Good for repo-only tasks, not GSD execution.
- CronCreate with `durable: true` persists to .claude/scheduled_tasks.json, survives restarts
- `/loop` and `/schedule` are built-in skills for orchestration
- `--bare` mode skips hooks/plugins/CLAUDE.md (saves ~49K tokens but loses toolkit)
- `--effort max` flag exists for maximum autonomy
- GSD execute-phase is already headless-compatible (no AskUserQuestion in core, TEXT_MODE flag exists)
- Token overhead is ~53K/session, 91% from Vercel plugin (disable for non-Vercel work)

Key files:
- references/autonomous-toolkit-research.md - THE comprehensive research doc (all findings, all 5 rounds of agents)
- rules/claude-behavior.md - toolkit behavior rules (philosophy embedded here)
- config/interfaces.json - operation decoupling (will need new operations)
- docs/v5-unified-design.md - v5 design doc (sections 1-6 and 12 for architecture)
- ~/.claude/get-shit-done/workflows/execute-phase.md - GSD execute (headless-compatible)
- ~/.claude/get-shit-done/workflows/autonomous.md - GSD autonomous workflow

Open architecture question:
The right path is likely auto mode + CronCreate durable (or /loop) as the orchestration layer,
NOT a bash wrapper. This keeps execution inside Claude Code sessions where toolkit hooks,
.planning/ state, and GSD integration all work natively. Need to design exactly how this
integrates with the toolkit's four-system architecture and interfaces.json decoupling.

Uncommitted changes: references/autonomous-toolkit-research.md (updated with resolved questions + round 5 sources)

Next step: Design the specific toolkit changes using first-party features (auto mode, CronCreate,
/loop) instead of a wrapper script. This means new operations in interfaces.json, possibly a new
workflow file, and hook integration for notifications.
