# Computed cache — regenerate with: tools/stack-metrics.sh --full-rebuild
checks/metrics.json

.DS_Store
*.tmp
.~lock.*

# Settings backups (machine-specific, not portable)
backups/

# Bun/Node
node_modules/
bun.lock
